# CLAUDE.md — 開發守則（所有在本 repo 工作的 Claude 都先讀這裡）

高雄榮民總醫院企業工會（籌備中）網站。純靜態多頁 HTML/CSS/原生 JS，部署在 GitHub Pages，**部署分支＝`main`**，推上去就自動上線。

## ⚠️ 絕對規則（避免回退使用者的修正）
1. **版號一律用 `./bump.sh`**：它會同步更新 `ver.txt` 與 `version.js`（單一真相源）。**不要手改版號、不要只改其中一個。**
2. **禁止「整檔覆蓋式同步」**：不要用整份檔案覆蓋的方式去「同步最新版」某頁——這會把先前一條一條的修正一次蓋掉（本專案發生過：把已隱藏的頁首圓圈又蓋回可見）。要改就**針對性地改那幾行**（Edit），不要 wholesale replace。
2a. **官方文件版面禁止自行改動**：凡是依照官方格式製作的表單／書表（如切結書、申請書等），**只能改文字內容，絕對不能動字型、字級、行距、行高、邊距、padding、版面配置**。本專案發生過：使用者給官方格式，Claude 卻自己換成「覺得好看」的版面，被使用者強烈反映「請不要自己瞎掰」。收到官方文件後，先問清楚格式，或嚴格照官方原稿複製，不要加任何美化。
3. **改完要 commit＋push 到 `main`**，commit 訊息用**中文**、結尾附「版本 <10碼>」。push 用 `git push -u origin main`，失敗指數退避重試。
4. **回覆使用者時，最後一定附上線上網址**（使用者很在意）。
5. **連結一律給「裸網址」**（`https://…` 單獨一行，手機 App 會自動變可點）。**不要用 Markdown `[文字](網址)` 格式**——使用者的 App 不會渲染，反而更難點/複製（使用者多次強烈反映過）。
6. **只要你請使用者「去操作某功能」或「去看某結果」，就一定要附上那一頁的裸網址**（讓他直接點進去、不用自己找）。反之，沒有要他去操作/查看時，不要無故丟一長串連結清單叫他複製（手機複製很痛苦，使用者多次抱怨）。原則：**該給的時候一定給、不該給的時候別亂貼。**

## 架構重點
- 無後端。資料只存本機 localStorage/IndexedDB，**會員 PII 絕不上傳**。
- `sw.js`：`importScripts("./version.js")`→`CACHE="ebn-"+self.__BUILD`。**網頁與核心程式（version/common/law-tips/finance-law.js、common.css）走「網路優先」**，其餘靜態快取優先；ver.txt/laws.json/laws-extra.json 不攔。（這是修「改了沒反應/更新不掉」的關鍵，別改回快取優先。）
- 版本顯示格式：去世紀點分隔 `v26.06.22.30`。
- `common.css`＋`common.js` 載入全部頁。
- 頁首**不要再加裝飾圓圈**（`.header::before/::after` 的 border-radius:50%）——使用者要求全站拿掉、已設 display:none。

## 站點
- `index.html`＝工會首頁（綠）；`jiaban.html`＝加班費說明；`gaorong.html`＝轉址→`./`。
- `union.html`＝幹部功能表（深藍）；`meeting.html`＝會議文件（金，含掃碼簽到）；`roster.html`＝會員名冊。
- 返回鈕：公開區子頁→「🏠工會首頁」index.html；幹部區子頁→「‹功能表」union.html。

## 幹部名冊（cadres 系統）
- **幹部鍵值（8/9 起 6 個）**：`chair` 理事長・`fin` 總務・`doc` 法規・`wel` 文宣・`org` 組訓・`sup` 監事。
  - ⚠️ **`fin2`／`doc2` 這兩個 key 已經整個廢除、不是留白**：原本「總務（一）／總務（二）」「法規（一）／法規（二）」的雙人編號制，使用者 8/9 要求「一二取消了，直接打名字，不用順位了」，所以把 fin2 併回 fin（蔣雅竹）、doc2 併回 doc（巫佳容），兩個 key 從 `org.js` CADRE_ROLES/CADRE_DEFAULTS 整個刪掉，`nav.html`／`voiceguide.html` 對應的重複卡片／語音腳本也一併刪除合併。**不要看到舊 commit 或截圖裡還有 fin2/doc2 就以為要補回來**——那是改制前的舊結構。
  - **`org` 組訓是後來才升格成正式幹部 key 的**：原本只是 `union.html` 裡一段純文字說明（沒有連到 org.js），使用者事後強調「組訓是正式幹部編制，主要以陳依婷為主，理事長已經很忙了」，所以把它比照其他角色升格成完整的 CADRE_ROLES 項目（`org.js`/`union.html`/`nav.html`/`voiceguide.html`/`cadres.html` 都補齊），值為陳依婷單獨一人，**不再掛楊淯涵**（她本來是理事長兼著組訓，這次明確拆開）。
  - 受影響、已同步的檔案：`org.js`（CADRE_ROLES/CADRE_DEFAULTS）、`union.html`（syncPair 改回單純 fillName，組訓卡改用 `id="name-org"` 動態帶入）、`petition.html`、`founders.html`、`pay.html`、`cadres.html`（ROLES_META＋badge CSS）、`nav.html`（卡片＋CSS 顏色）、`voiceguide.html`（語音腳本＋roleKeys 陣列）。
- **改名流程**：在 `org.html`「幹部名單」區修改 → 按「儲存與發布」→ 發布的 `public.json` 含 `cadres` 欄位 → 全站（nav.html / voiceguide.html / gongwen.html 等）自動讀取 `EBNOrg.cadreName(key)`。
- `org.js` 是幹部/組織資料單一真相源：`CADRE_DEFAULTS`（內建值）＋`localStorage ebn_cadres_v1`（草稿）＋`public.json cadres`（已發布）三層合併，已發布優先。使用者瀏覽器 localStorage 草稿裡如果還殘留舊的 fin2/doc2 key，不會出錯，就只是沒有任何程式碼再讀取它，屬於無害孤兒資料。
- `voiceguide.html` 語音腳本裡的名字**不改**（腳本已個人化如「楊淯涵，你是理事長」）；只有 ROLES[key].name（標題顯示）會由 syncNames() 自動更新。

## 深層實測（凡大功能必做）
- 測試要用 `elementFromPoint` 確認元素真實可點（不被遮擋），不能只看 DOM 存在。
- 幹部導覽說明框不能擋住示範內容：改用瞬間捲動＋說明框貼螢幕遠側。
- 送出/行動按鈕必須在手機視窗內（y < 844）。
- 新功能測試項目加進 `tests/e2e.cjs`，`await T(描述, async()=>{...})` 格式。
- 全站改動後跑 `bash tests/run.sh`（40 項全過才 push）。

## 使用者溝通習慣（重要）
- 使用者用手機 App 看回覆，Markdown `[文字](網址)` 會顯示原始碼而非連結。裸網址 `https://...` 才能點。
- 有功能要讓使用者去試：**一定附那頁的裸網址**（不用他自己找）。
- 沒有要他去操作時，**不要無故列一堆網址**（手機複製很痛苦）。
- 每次回覆結尾附線上網址（使用者很在意）。
- **【鐵律】客觀中立、不迎合、不提供情緒價值**：回覆優先給客觀分析；使用者表達裡有邏輯漏洞、認知偏差、或站不住腳的地方，直接指出來，越直接越好，不要包裝、不要安撫。不可逆或影響範圍大的操作（刪資料、改密碼機制、動 CI 設定等）仍先確認再動手——這是風險控管，不算違反本條。

## 測試（沙箱）
CDN（qrcodejs/pptxgenjs）被擋但正式站正常。本機用 `python3 -m http.server`＋Playwright：chrome=`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`，playwright-core=`/opt/node22/lib/node_modules/playwright/node_modules/playwright-core`。
**自動化測試：`bash tests/run.sh`**（自動開伺服器→跑端對端→收尾；全過 exit 0、有錯 exit 1）。改完程式想確認沒弄壞別頁就重跑它；新增測試在 `tests/e2e.cjs` 照 `await T(...)` 格式加。⚠️ 別用 `pkill`（會殺掉自己的 shell→exit 144）；要換埠用 `PORT=8170 bash tests/run.sh`。

## 📋 最近進度
<!-- AUTO:START -->
最後更新：2026-08-09

最近 commits：
  f2278fb 自動更新 CLAUDE.md 最近進度（Stop hook）
  c6d6c4c 組訓升格為正式幹部編制（org key），主要由陳依婷負責，不再跟理事長楊淯涵並列（理事長已經很忙）。org.js新增CADRE_ROLES/CADRE_DEFAULTS，union.html組訓卡改動態帶入，nav.html新增組訓卡片與配色，voiceguide.html新增組訓語音腳本，cadres.html補ROLES_META與badge樣式
  86d8b0f 自動更新 CLAUDE.md 最近進度（Stop hook）
  18902ee 自動更新 CLAUDE.md 最近進度（Stop hook）
  2ce8b08 幹部職務取消「一/二」順位編號制：總務、法規不再各設兩個順位，直接掛名字。fin2/doc2 兩個key從org.js CADRE_ROLES/CADRE_DEFAULTS整個刪除，nav.html/voiceguide.html移除重複卡片與語音腳本，union.html/pay.html/petition.html/founders.html/cadres.html同步簡化
<!-- AUTO:END -->

### ⚖️ 法規鐵則（踩過的雷，別再犯）
- **官方參考手冊在 repo 裡**：`reference/工會組織運作實務參考手冊-純文字檔.txt`（勞動部體系，112年11月）。**要做工會書表/辦法前先 grep 這份**，裡面有委託書、簽到冊、會議紀錄、選舉票、選舉辦法的官方範本。不要自己造輪子、不要憑印象寫（使用者明確說過「不要自己硬掰一份給我」）。
- **經常會費不得低於當月工資 0.5%**（工會法第28條第2項），入會費不得低於入會時一日工資。**寫死固定金額幾乎都違法**（本專案發生過：250元不管當月費或年費，對多數薪資都低於法定下限）。章程第26條要用浮動制。
- **委託出席是「法定預設可行」**：另有《工會會員大會或會員代表大會委託出席辦法》（依工會法27條2項訂定），**章程沒寫也自動適用**——本專案發生過：誤判「章程沒設條款＝不能委託」，寫進選舉辦法後被推翻重改。委託上限＝親自出席人數 1/3，主席宣布開始選舉後不得再委託。
- **兩份章程並存要小心**：`charter.html`（33條新版，費額用百分比）vs `shouce.html`/`prep2.html` 附件二（47條版，曾寫死金額）。改費額/名額時**兩邊都要對齊**。

### 已知既有測試失敗（不是你弄壞的）
`bash tests/run.sh` 目前固定 41 過 1 失：`wenhao — next=高總籌工字第115002號`（文號流水簿）。改動前後都一樣失敗，與新功能無關；push 前確認「只有這一項」失敗即可。

### 本 session 工作紀要（2026-07-12～07-29）
**成立大會文件（8/30）——`shouce.html` 為主：**
- 新增**附件七《理監事選舉辦法草案》**：照官方手冊選舉篇範本填本會實數（理事7＝正取5備取2、監事2＝正取1備取1），含登記期限(8/20)、資格審查抽籤(8/23)、公布(8/24)、13款無效票認定、正備取抽籤
- 新增**附件八《選舉投開票作業事項及用品清單》**：選票/票匭/封條/領票名冊/計票單/封票單/遮蔽圈選處打勾清單＋官方10步驟投開票流程（補上長期缺的「實體物品」缺口）
- 議程補上**互選理事長**（章程第18條要求，原本只寫入會申請書審查）
- **簽到與領票不再重疊**：14:00報到只簽到領議程；領票併到選舉開始前，與領票名冊簽章一次辦完
- 時程重編：開票計票 15→20 分鐘，並排 **15:23-15:33 機動緩衝**吸收超時，總長仍 120 分鐘

**候選人名單自助編輯（使用者要求「不要每次都透過你」）：**
- `org.js` 新增 `candidates()`/`setCandidates()`（key `ebn_candidates_v1`），**草稿優先於已發布**（與 `get()` 同邏輯，避免發布後本機新增被蓋掉）
- `org.html` 新增「🗳 理監事候選人名單」卡片，可自由增/刪/改姓名經歷政見
- `shouce.html` 柒節選舉公報表格（`#cand-directors-body`/`#cand-supervisors-body`）自動帶入；沒資料就保留空白列供列印
- 已 Playwright 實測全鏈路（新增→改→刪→跨頁同步）通過

**委託出席（8/30 只有15人親自出席，30名會員需過半16人才能開會）：**
- `meeting.html` 委託書改為官方格式（全權/部分委託勾選、受託編號、5款注意事項）
- 簽到表補「委託出席（受託人）簽到」「委託序」欄；會議紀錄拆親自/委託/請假/缺席四類＋散會時間＋選舉異議記載
- 附件七第4條、柒節選舉資格改為「得委託，上限親自出席1/3」

**其他：**
- `prep3.html` 第三次籌備委員會（7/26中午）議程；`charter.html` §26 加月薪試算（輸入月薪即算0.5%金額）
- `beike-prep.html` 貝殼集器/PAYUNi 申請預備表（成立後總務自行填，不存理事長身分證等PII）
- Word 產檔腳本在 scratchpad（容器重置會消失，需要時重寫）：`make_shouce_docx3.py` 等，用 python-docx，14pt標楷體/行距1.8

### 本 session 工作紀要（2026-07-30～07-31）
**這個 session 是被系統設定成獨立分支 `claude/union-3-1-u9j828`（不是照 CLAUDE.md 慣例直接 push main）**，使用者事後明確要求「push 到 main 讓它上線」才 merge 過去，所以本 session 的每筆修正都是先在該分支 commit，再 merge 進 main 並 push。目前 main 已是最新（含以下全部）。

**籌備委員名單變動（重要，之後每個 session 都要以此為準）：**
- 原 7 人籌備委員：楊淯涵、莊瑋聆、蔣雅竹、巫佳容、葉柏宏、黃淑姸、陳依婷
- **改為 6 人**：楊淯涵、巫佳容、黃淑姸、陳依婷、黃敏（新加入）、蔣雅竹
- ⚠️ **姓名拼字這兩位來回改過好幾次，之後直接信這裡、不要再猜**：**蔣雅竹**（不是「蔣雅」——8/1 一度被誤訂正成兩字「蔣雅」，8/2～8/3 改回一次，8/9 這個 session 又被要求改成「蔣雅」、旋即使用者親自確認「蔣雅從頭到尾都是錯的，是蔣雅竹才對」再次改回，commit `2784423` 與本次 8/9 的姓名修正 commit 皆為蔣雅竹版本）；**黃淑姸**（不是「黃淑妍」，commit `6272456` 為最終版）。全站已統一。
- **幹部職務（8/9 已定案，不再是「還沒決議」；之後又追加一次異動，一樣要以此區塊最新版為準）**：莊瑋聆、葉柏宏退出籌備委員後，兩人原本的幹部職務（`org.js` CADRE_DEFAULTS：`fin` 總務（一）＝莊瑋聆、`doc2` 法規（二）＝葉柏宏）曾一度懸而未決；8/9 使用者先確認：**總務（一）＝莊瑋聆（留任）**、**監事＝黃敏（原監事陳依婷改任一般理事，不再兼監事）**、**法規（二）已決議取消，不再設此職務**（不是「還在找人補」，是確定不補了）、**陳依婷改編入組訓組**（跟楊淯涵一起）。**同一天稍後，莊瑋聆又從總務完全退出**（使用者原話「莊瑋聆刪除理事長職位」，經追問確認實際意思是「總務只剩蔣雅竹」，跟理事長楊淯涵無關、是使用者當下打字不清楚，不是真的要動理事長職位），所以**總務（一）現在也是出缺**，莊瑋聆目前不掛任何幹部職務。
  - `org.js` CADRE_DEFAULTS 這行後來又變過一次：8/9 稍晚莊瑋聆從總務完全退出、且使用者要求「一二順位取消，直接打名字」，把 fin/fin2、doc/doc2 的雙人編號制整個廢除，`fin2`／`doc2` 這兩個 key 已從系統刪除（詳見上面「幹部名冊（cadres 系統）」章節），**目前現況見那一節，不要再照這裡曾經出現過的 fin2/doc2 舊結構去改東西**。
- 已同步這次幹部異動的檔案：`org.js`、`union.html`（分工說明列＋總務卡＋監事卡＋組訓卡）、`nav.html`（fin／sup 卡片）、`voiceguide.html`（fin／sup 語音腳本 fallback）、`workplan.html`（理監事欄＋監察稽核負責人）、`cases.html`（案件負責人下拉新增黃敏）、`budget-draft.html`（監事欄）。陳依婷本人**沒有**從任何名單移除，她還是會員/理事，只是不再掛「監事」頭銜、改掛組訓；莊瑋聆本人也**沒有**被除名，只是目前不掛任何幹部職稱，這些名字還會出現在籌備委員歷史紀錄／連署人名單等一般名單裡，不要看到她們名字出現在別處就以為又要改回去。
- 已改的檔案（籌備委員現況名單）：`shouce.html`「貳、籌備委員名單」、`prep3.html` 出席欄、`qiejieshu.html` 連署人名單、`prep2.html`/`shouce.html` 附件三（歷史出席紀錄，只改姓名拼字沒改名單組成）。

**第三次籌備委員會改期到 8/9（原訂 7/26 沒開成）：**
- `prep3.html` 更新：日期改 115年8月9日（時間、地點欄位還空著，寄出前要填）；委員名單同步新 6 人版
- 議程新增第六段落「召開成立大會前之準備工作檢核」，逐項對照官方籌組工會指引（場地已定耀嵩豐、開會通知已含理監事互選、大會手冊編印中、選舉用品待採購、聯合組織提案未討論）
- 提案討論擴充到**案由一～七**，把所有還沒決定的事全部變成 8/9 當天要現場決議的議程項目，不需要使用者現在先回答：
  1. 章程英文名稱、會址確定
  2. 理監事選舉辦法草案確認
  3. 成立大會工作人員分工（司儀/紀錄/報到組/選務組/攝影，全空待指派）
  4. 理監事候選人登記受理窗口
  5. 投開票用品準備（採購/製作人員）
  6. 是否參加所屬區域或行業之聯合工會組織
  7. 莊瑋聆/葉柏宏退出後幹部職務怎麼處理、黃敏補哪個職務
- 已產出對應 Word 檔（用 npm `docx` 套件而非之前的 python-docx；腳本在 scratchpad `make_prep3_docx.js`，容器重置會消失）並傳給使用者
- ⚠️ **這次 sandbox 的 LibreOffice（soffice）轉檔壞掉**：`soffice --headless --convert-to pdf` 連空白 docx 都轉不了（"source file could not be loaded"，換乾淨 profile／不同檔名都一樣），沒辦法在容器內肉眼驗證 docx 排版，是環境問題不是文件內容錯誤；使用者要自己在 Word 打開確認排版。
- 引註來源要小心：使用者提供的照片（籌備會職責 5 項清單）**不是**來自 repo 裡現有的 `reference/工會組織運作實務參考手冊-純文字檔.txt`（該手冊第14頁其實是「提案單範例」，內容完全不同）；目前文件裡引註已改成中性的「官方籌組工會指引」，沒有寫死錯誤的手冊名稱。若之後查到照片實際出處，要回頭把引註寫精確。

**其他修正：**
- `prep2.html` 附件二預算表筆誤：「40人*125元」漏打一個 0，應為「40人*1250元」（金額欄 50000 本來就是照 1250 元/人算）

**目前版本：main 已到 2026073103。**
